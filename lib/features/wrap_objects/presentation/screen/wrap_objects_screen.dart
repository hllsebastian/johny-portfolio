import 'package:flutter/material.dart';

class WrapObjectsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Juego de Figuras')),
      body: Center(child: Text('TODO here the wrap game')),
    );
  }
}
